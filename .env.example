# ============================================================
# Priority Commander — Production Environment Variables
# Copy this to .env and fill in your values
# ============================================================

# ── Appwrite (baked into client at build time) ──
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://login.trendss.net/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=69986d6b00089ab44a8d
NEXT_PUBLIC_APPWRITE_DATABASE_ID=priority-commander

# ── Redis Cache ──
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=your-strong-redis-password-here

# ── Security: Allowed Origins (comma-separated) ──
# Only these domains can access API routes
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ── App Port ──
APP_PORT=3000
